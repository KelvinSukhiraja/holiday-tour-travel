import { useState, useEffect } from "react"; import { NavLink, Link } from "react-router-dom"; import { navItems } from "@/lib/utils"; import MobileMenu from "./ui/mobile-menu"; export function Navbar({ theme }: { theme: "light" | "dark" }) { const [show, setShow] = useState(true); const [lastScrollY, setLastScrollY] = useState(0); const [scrolled, setScrolled] = useState(false); const themeText = theme === "light" ? "text-white-a" : "text-A"; const themeBg = theme === "light" ? "bg-A" : "bg-white-a"; const themeLogo = theme === "light" ? "/LOGO-WHITE.png" : "/LOGO.png"; useEffect(() => { const handleScroll = () => { const currentScrollY = window.scrollY; const heroHeight = window.innerHeight; if (currentScrollY < heroHeight) { setShow(true); setScrolled(false); } else { if (currentScrollY > lastScrollY) { setShow(false); } else { setShow(true); } setScrolled(true); } setLastScrollY(currentScrollY); }; window.addEventListener("scroll", handleScroll); return () => window.removeEventListener("scroll", handleScroll); }, [lastScrollY]); return ( <header className={absolute w-full flex justify-between items-center py-6 px-8 md:px-32 z-40 transition-all duration-300 ${show ? "translate-y-0" : "-translate-y-full"} ${scrolled ? themeBg : "bg-transparent"} } > <Link to="/" className=""> <img src={themeLogo} className="w-36" loading="lazy" /> </Link> <nav className="hidden md:flex items-center justify-around gap-20"> {navItems.map((item) => ( <NavLink key={item.href} to={item.href} end={item.href === "/"} // exact match for root link className={({ isActive }) => fourth-text font-medium ${themeText} relative after:content-[""] after:absolute after:left-0 after:-bottom-1 after:h-[2px] after:rounded-full after:transition-all after:duration-300 ${isActive ? "after:w-full after:bg-A" : "after:w-0 after:bg-transparent"} hover:after:w-full hover:after:bg-A } > {item.label} </NavLink> ))} </nav> <MobileMenu theme={theme} /> </header> ); }